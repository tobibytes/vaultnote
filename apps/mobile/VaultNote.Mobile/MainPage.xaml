<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VaultNote.Mobile.MainPage">

    <ScrollView>
        <VerticalStackLayout Padding="24" Spacing="16">
            <Label
                Text="VaultNote"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Personal knowledge vault"
                Style="{StaticResource SubHeadline}" />

            <Label
                Text="Notes"
                FontAttributes="Bold"
                FontSize="14"
                TextColor="{StaticResource Gray600}" />

            <Button
                Text="Create Note"
                Clicked="OnCreateNoteClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="List Notes"
                Clicked="OnListNotesClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Search Notes"
                Clicked="OnSearchNotesClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Ask Vault"
                Clicked="OnAskVaultClicked"
                HorizontalOptions="Fill" />

            <Label
                Text="Account"
                FontAttributes="Bold"
                FontSize="14"
                TextColor="{StaticResource Gray600}" />

            <Button
                Text="Login"
                Clicked="OnLoginClicked"
                HorizontalOptions="Fill" />

            <Button
                Text="Register"
                Clicked="OnRegisterClicked"
                HorizontalOptions="Fill" />

            <Label
                Text="Diagnostics"
                FontAttributes="Bold"
                FontSize="14"
                TextColor="{StaticResource Gray600}" />

            <Button
                x:Name="PingButton"
                Text="Ping Backend"
                Clicked="OnPingClicked"
                HorizontalOptions="Fill" />

            <Label
                x:Name="PingStatusLabel"
                Text="No ping sent yet."
                FontSize="16"
                LineBreakMode="WordWrap" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
